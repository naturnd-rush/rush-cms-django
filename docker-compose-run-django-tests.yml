# Compose file that specifies test commands for the rush admin site

services:
  django:
    command: >
      sh -c "poetry run python manage.py migrate &&
            python manage.py showmigrations --plan && 
            poetry run pytest"
